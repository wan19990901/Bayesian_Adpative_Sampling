"""Inference utilities for data loading, generation, and evaluation.

Subpackages are intentionally not imported eagerly so optional dependencies
such as vLLM, Gemini, and AlpacaEval are only required by the commands that
use them.
"""
